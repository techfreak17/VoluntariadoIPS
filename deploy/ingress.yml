apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: name-virtual-host-ingress
spec:
  rules:
  - host: voluntariadoips.gp.pt
    http:
      paths:
      - backend:
          serviceName: service1
          servicePort: 80